arch: amd64
os: linux
dist: focal

language: node_js
node_js:
  - "node"

addons:
  apt:
    sources:
      - google-chrome
    packages:
      - google-chrome-stable

cache:
  directories:
    - ./node_modules

install:
  - npm install

script:
  - npm run test -- --no-watch --no-progress --browsers=ChromeHeadlessCI
  - ng build --prod

deploy:
  provider: npm
  email: "rahim.alizada@gmail.com"
  api_token: "6d9f3165-2f79-42d2-aa70-54f30ccb4831"
  on:
    tags: true
